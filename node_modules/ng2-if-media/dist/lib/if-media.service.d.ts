export declare class IfMediaService {
    private elements;
    private reflections;
    private throttle;
    private isThrottling;
    private resized;
    private notifyTimeout;
    private parser;
    constructor(config: any);
    register(component: any): {
        when: Function;
        onChange: Function;
        deregister: Function;
    };
    isMedia(query: any): boolean;
    private sendNotifications();
    private throttledNotify();
    private onResize();
    private addSingleReflection(container, {query, matchFn, onlyWhenMatched, newState});
    private addObjectReflection(container, queryObj, onlyWhenMatched?);
    private mergeNewState(component, newState);
    private removeReflection(container);
    private resolveReflections();
    registerElement(element: any, query: string): void;
    deregisterElement(element: any): void;
    private notifyElements();
}
