import { OnDestroy, OnInit, TemplateRef, ViewContainerRef } from '@angular/core';
import { IfMediaService } from './if-media.service';
export declare class IfMediaDirective implements OnDestroy, OnInit {
    private templateRef;
    private viewContainer;
    private mediaService;
    private media;
    private hidden;
    ngIfMedia: string;
    ifMedia: string;
    constructor(templateRef: TemplateRef<any>, viewContainer: ViewContainerRef, mediaService: IfMediaService);
    ngOnInit(): void;
    hide(): void;
    show(): void;
    ngOnDestroy(): void;
}
